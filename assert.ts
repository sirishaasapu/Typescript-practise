//type asseration or type casting :if don't konw the type of variable you want to make it string
//we  use ***as string ***to convert or ***<string>***
let animall :any="i am a dog";
let anii=(animall as string).length;//here we cast it as a string
//            (or)
//let anim=(<string>animall).length;//we can also use <string> cast it as string
